/**
 * Optional Fastify HTTP server.
 * Install fastify separately to use: npm install fastify
 * Then run: node dist/server.js
 */

// This file is intentionally left as a stub.
// To enable the HTTP server, install fastify and uncomment the implementation.
export {};
